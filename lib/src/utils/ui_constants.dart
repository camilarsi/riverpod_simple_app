import 'package:flutter/cupertino.dart';

class UiConstants {
  static const mainText = Text(
    UiConstants.homeTitle,
    textAlign: TextAlign.center,
    style: TextStyle(
      fontSize: 36,
    ),
  );
  static const sortingButtonText = 'Know your destiny';
  static const homeTitle = "What are we having for dinner? ";
  static const cardNameTextStyle = TextStyle(fontSize: 45);
  static const cardEmojiTextStyle = TextStyle(fontSize: 170);
}
